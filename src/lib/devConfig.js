// File: /src/lib/devConfig.js

/**
 * @fileoverview Developer-specific configuration for testing purposes.
 * Allows toggling dynamic authorization without modifying environment variables.
 */

/**
 * Flag to enable dynamic authorization during development.
 * Set to true to test dynamic authorization features.
 * @type {boolean}
 */
export const DEV_USE_DYNAMIC_AUTHORIZATION = false; // Change to true for testing
